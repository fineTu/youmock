// Generated by CoffeeScript 1.9.3
(function() {
  var ImageAnnotation,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  ImageAnnotation = (function(superClass) {
    extend(ImageAnnotation, superClass);

    function ImageAnnotation() {
      return ImageAnnotation.__super__.constructor.apply(this, arguments);
    }

    ImageAnnotation.prototype.name = 'img-annot';

    ImageAnnotation.prototype.icon = 'img-annot';

    ImageAnnotation.prototype.htmlTag = 'p';

    ImageAnnotation.prototype.cssName = 'img-annot';

    ImageAnnotation.prototype.status = function($node) {
      if ($node != null) {
        this.setDisabled($node.is(this.disableTag));
      }
      if (this.disabled) {
        return true;
      }
      return $node.hasClass(this.cssName);
    };

    ImageAnnotation.prototype.command = function(param) {
      var $contents, $endBlock, $startBlock, endNode, j, len, node, range, ref, results, startNode;
      range = this.editor.selection.getRange();
      startNode = range.startContainer;
      endNode = range.endContainer;
      $startBlock = this.editor.util.closestBlockEl(startNode);
      $endBlock = this.editor.util.closestBlockEl(endNode);
      this.editor.selection.save();
      range.setStartBefore($startBlock[0]);
      range.setEndAfter($endBlock[0]);
      $contents = $(range.extractContents());
      results = [];
      $contents.children().each((function(_this) {
        return function(i, el) {
          var c, converted, j, len, results1;
          converted = _this._convertEl(el, param);
          results1 = [];
          for (j = 0, len = converted.length; j < len; j++) {
            c = converted[j];
            results1.push(results.push(c));
          }
          return results1;
        };
      })(this));
      ref = results.reverse();
      for (j = 0, len = ref.length; j < len; j++) {
        node = ref[j];
        range.insertNode(node[0]);
      }
      this.editor.selection.restore();
      return this.editor.trigger('valuechanged');
    };

    ImageAnnotation.prototype._convertEl = function(el, param) {
      var $block, $el, results;
      $el = $(el);
      results = [];
      if ($el.is(param)) {
        results.push($el);
      } else {
        $block = $("<p class='" + this.cssName + "'/>").append($el.contents());
        results.push($block);
      }
      return results;
    };

    return ImageAnnotation;

  })(Simditor.Button);

  Simditor.Toolbar.addButton(ImageAnnotation);

}).call(this);
